# Plan: M3.2 stm32 平台篇

> Created: 2026-06-05
> Skill: ai-engineer-workflow-v5
> 流程裁剪:沿用 M3.1(跳过 OpenSpec 变更)
> 模板来源:ADR-004(`openspec/specs/architecture/spec.md`)
> 前置:learn/docs/08-hal-architecture.md(M3.1 已完成)

---

## 1. 概览

| 项 | 内容 |
|---|------|
| 产出 | `docs/09-stm32.md`(500-800 行,7 节,ADR-004 模板) |
| 不在范围 | M3.3 nrf / M3.4 rp(下轮)、M4 外设详解(后续里程碑) |
| 依赖 | M1 / M2 / M3.1 + ADR-004 |
| CodeGraph | 用 ADR-004 §5 "入口清单"复用 |
| 预估总时 | A: 探索 45m · B: 起草 + Write 2-3h · C: 收尾 15m |

---

## 2. 探索清单(ADR-004 §5 入口复用 + stm32 专属)

### 复用 ADR-004 入口(平台无关)

- `Peri` / `PeripheralType`(M3.1 已探)
- `interrupt::typelevel::{Interrupt, Handler, Binding}`(M3.1 已探)
- `Driver` trait(M3.1 已探)

### 新探索:stm32 特定

| # | 关注点 | 工具 / 路径 |
|---|--------|-------------|
| 1 | `embassy-stm32/src/lib.rs` init 入口 | Read(精确定位 init 函数) |
| 2 | `Config` 结构 / 时钟树 | `codegraph_node Config`(stm32 范围)+ Read `rcc/mod.rs` |
| 3 | `interrupt_mod!` 在 stm32 的展开 | Read `embassy-stm32/src/interrupt.rs` 或类似 |
| 4 | time-driver-* feature 矩阵 | `grep "time-driver" embassy-stm32/Cargo.toml` |
| 5 | `embassy-stm32/src/time_driver/` 目录 | `codegraph_files` 看模块结构 |
| 6 | DMA 抽象(BDMA / GPDMA) | `codegraph_files embassy-stm32/src/dma` |
| 7 | `stm32-data` 与 metapac 关系 | Read `embassy-stm32/Cargo.toml`(看 metapac 依赖)+ 看 build.rs |
| 8 | `peripherals!` 宏 / `pin_trait!` 宏 | `embassy-stm32/src/macros.rs`(已在 M3.1 探索时看了一部分) |
| 9 | GPIO Speed 等 stm32 独有 | `Read embassy-stm32/src/gpio.rs`(M3.1 已部分看) |

**预算**:codegraph_explore ≥2 次、codegraph_files ≥2 次、codegraph_node/search ≥3 次、Read 关键文件 ≥4 次。

---

## 3. Phase A: 探索(45m)

### Step A1: 平台总览(15m)
- `codegraph_files embassy-stm32/src`(顶层结构)
- `codegraph_files embassy-stm32/src/rcc`(时钟树代码量级)
- `codegraph_files embassy-stm32/src/time_driver`(时间驱动选项)
- Read `embassy-stm32/Cargo.toml`(features 一览,16+ 系列、time-driver-*)
- Read `embassy-stm32/src/lib.rs`(init / Config)

### Step A2: stm32-data 与 metapac(10m)
- `codegraph_search "stm32_metapac"`(看依赖关系)
- 看 `embassy-stm32/build.rs`(metapac 生成时是否参与)
- 网上常识 + 注释复述 stm32-data → stm32-metapac 流程(不展开成独立工程)

### Step A3: 中断模型与 GPIO 补探(10m)
- Read `embassy-stm32/src/gpio.rs` 1-100 行(Pull/Speed/Pin trait)
- `codegraph_node Output`(stm32 范围,看完整)
- Read `embassy-stm32/src/macros.rs`(peripherals!/bind_interrupts!/peri_trait! 等)

### Step A4: DMA 抽象(10m)
- `codegraph_files embassy-stm32/src/dma`
- 看 dma/mod.rs 暴露的关键 API(`Channel` trait?Burst?)

### Verify A
- 探索清单 9 项全覆盖
- 关键符号有 `file:line` 定位

---

## 4. Phase B: 起草 + Write(2-3h)

### Step B1: 章节细化(30m)
对每节先列要点(模板内填):
- 1 节:芯片家族 + 应用场景
- 2 节:stm32-data 概念 → metapac 生成 → HAL 用法(3 层)
- 3 节:init() 流程 + Config 时钟树 + Peripherals 单例
- 4 节:接 M3.1 §5,展示 stm32 实例(InterruptHandler<T>、SAADC 例子的 stm32 等价)
- 5 节:time-driver-* feature 矩阵 + tim1/2/.../rtc 选择策略 + 实现位置
- 6 节:GPIO Flex/Input/Output(M3.1 已展示过基础)+ Speed/Pull 完整 + EXTI(GPIO 中断)
- 7 节:BDMA/GPDMA + 世代差异(F1/F4/H7 区别)+ HAL 模块概览(spi/uart/i2c/adc/can/usb 等)

### Step B2: Write 第 1-3 节(40m)
- `Write docs/09-stm32.md` 写 1-3 节
- 预估 ~250 行

### Step B3: Edit 追加 4-5 节(40m)
- 预估 +200 行

### Step B4: Edit 追加 6-7 节 + 参考(30m)
- 预估 +200-250 行
- 总计 ~600-700 行

### Step B5: 自验(15m)
- `wc -l docs/09-stm32.md`(500-800)
- `grep -c "^## " docs/09-stm32.md`(≥7 节 + 目录 + 参考)
- `grep -cE "\\.rs:[0-9]+" docs/09-stm32.md`(≥10)
- `grep -c '```mermaid' docs/09-stm32.md`(≥1)
- `grep -c '```rust' docs/09-stm32.md`(≥10)
- python emoji 扫描(0 次)
- 对照 M3.1 风格

---

## 5. Phase C: 收尾(15m)

### Step C1: tasks.md
- M3.2 状态 待办 → (已完成)
- M3 进度 1/4 → 2/4 (50%)
- 总计 8/27 → 9/27 (30% → 33%)
- 已完成区追加 M3.2

### Step C2: SNAPSHOT.md
- 当前阶段:M3.2 完成
- 下一步:M3.3 nrf / M3.4 rp

### Step C3: learned/spec.md
- stm32 关键符号入口
- stm32-data 设计哲学速记
- time-driver 选择策略

---

## 6. Requirements Traceability Matrix

| Req | 描述 | 落实 | Status |
|-----|------|------|--------|
| R1 | 行数 500-800 | B2-B4 + B5 | (计划) |
| R2 | 严格 7 节模板(ADR-004) | B1 | (计划) |
| R3 | 每节 ≥1 处源码引用 | B2-B4 | (计划) |
| R4 | ≥1 Mermaid 图(推荐:stm32-data 生成流程) | B2 | (计划) |
| R5 | 0 emoji | B2-B4 + B5 | (计划) |
| R6 | 术语与 M3.1/ADR-004 一致 | B1 | (计划) |
| R7 | 第 6 节 GPIO 不重复 M3.1 §7 | B4 | (计划) |
| R8 | tasks/SNAPSHOT/learned 同步 | C1-C3 | (计划) |

**Gate 2 自检**:全 (计划),无 (跳过)、无 (缺失)。

---

## 7. 风险与应对

| ID | 风险 | 应对 |
|----|------|------|
| RK1 | stm32-data 是独立 repo(github.com/embassy-rs/stm32-data),代码不在本仓库 | 仅引用其概念,不深挖代码;通过 `embassy-stm32/Cargo.toml` 的 metapac 依赖说明关系 |
| RK2 | stm32 时钟树极复杂,容易跑题 | 只点出 Config 体系,具体 H7 时钟示例链接到 examples/ |
| RK3 | DMA 章节展开太多 | 限定 1 节 ≤80 行,重点放抽象层而非具体 channel |
| RK4 | time-driver-* feature 数量太多(16+) | 用表格列出代表性 3-5 个,其余说"详见 Cargo.toml features 段" |
| RK5 | 第 6 节 GPIO 与 M3.1 §7 内容重复 | 明确 split:M3.1 讲跨平台一致性,本节只讲 stm32 独有(Speed/EXTI/AFIO) |

---

## 8. Phase 边界 commit 建议(用户决定何时执行)

| 时机 | 建议 commit |
|------|-------------|
| Phase B 完成 | `docs(M3.2): stm32 平台(docs/09-stm32.md, N 行 7 节)` |
| Phase C 完成 | `chore(docs): M3.2 收官同步(tasks/SNAPSHOT/learned)` |

(workflow 不主动 git commit)
